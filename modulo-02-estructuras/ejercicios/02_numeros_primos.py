"""Ejercicio 02: Números primos hasta un límite.

Devuelve la lista de números primos entre 2 y `limite` (inclusive). Un número
es primo si solo es divisible entre 1 y él mismo.

Pista: implementa una función auxiliar `es_primo(n)` y úsala en una list
comprehension.

Si `limite` < 2, devuelve una lista vacía.

Ejemplos:

>>> numeros_primos(10)
[2, 3, 5, 7]
>>> numeros_primos(1)
[]
"""


def numeros_primos(limite: int) -> list[int]:
    """Devuelve los primos entre 2 y limite (inclusive)."""
    # TODO: implementar
    raise NotImplementedError
