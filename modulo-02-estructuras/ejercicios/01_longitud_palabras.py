"""Ejercicio 01: Longitud de palabras.

Dada una lista de palabras, devuelve un diccionario que mapea cada palabra
a su longitud. Resuélvelo con un *dict comprehension*, no con un bucle
clásico.

Si la lista contiene palabras duplicadas, la última gana (es el
comportamiento natural del dict comprehension).

Ejemplos:

>>> longitud_palabras(["python", "go", "rust"])
{'python': 6, 'go': 2, 'rust': 4}
>>> longitud_palabras([])
{}
"""


def longitud_palabras(palabras: list[str]) -> dict[str, int]:
    """Mapea cada palabra a su longitud."""
    # TODO: implementar con dict comprehension
    raise NotImplementedError
